# =====================================================
#  QuickBite — Root Configuration
# =====================================================
spring:
  application:
    name: quickbite-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api
